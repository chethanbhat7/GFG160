class Solution:
	def twoSum(self, arr, target):
		arr.sort()
		start=0;
		end=len(arr)-1;
		
		while(start<end):
	        s=arr[start]+arr[end]
		    if(s==target):
		        return True
		    elif(s<target):
		        start+=1
		    elif(s>target):
		        end-=1
	    return False
		       
		
