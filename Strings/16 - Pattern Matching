class Solution:
    def search(self, pat, txt):
        result=[]
        n,m=len(txt),len(pat)
        
        for i in range(n-m+1):
            if txt[i:i+m]==pat:
                result.append(i)
        return result
