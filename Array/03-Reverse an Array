class Solution:
    def reverseArray(self, arr):
        n=len(arr)
        left,right=0,n-1
        while left<right:
            arr[left],arr[right]=arr[right],arr[left]
            left+=1
            right-=1
        
        
        
