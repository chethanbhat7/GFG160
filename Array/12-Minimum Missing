class Solution:
    def missingNumber(self, arr):
        n=len(arr)
        temp=[0]*(n+2)
        for num in arr:
            if 1<=num<=n:
                temp[num]=1
        for i in range(1,n+2):
            if temp[i]==0:
                
                return i
                
