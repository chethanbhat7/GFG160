#User function Template for python3

class Solution:
    def findMin(self, arr):
        least=float('+inf')
        for i in arr:
            if i<least:
                least=i
        return least
